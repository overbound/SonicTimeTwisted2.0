if (!frozen)
{
if image_index == 4 {
    image_speed = 0;
}
if bubble_id.visible {
    y = ystart+cos(objScreen.image_index);
    
    }else {
    y=ystart;
    stop_sound(sndBubblesShake);
    }

}
