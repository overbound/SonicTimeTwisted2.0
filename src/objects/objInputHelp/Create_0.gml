/// @description  Alarms could have been used, but they don't account for the game being paused
visible = false;
duration = 120;
active = false;

in3d = instance_exists(objSSMessages);
hint = "placeholder";

